import React from 'react'

function Register(props) {
    let val;
    const fun = async ()=>{
        // val = await props.sign.greet();
    }
    fun()

  return (
    <div></div>
  )
}

export default Register